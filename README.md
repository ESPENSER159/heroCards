## How to Use

### Install dependencies

You can use one of them `npm`, `yarn`, `pnpm`, `bun`, Example using `npm`:

```bash
npm install
```

### Run the build

```bash
npm run dev
```

### Run project

```bash
npm run start
```